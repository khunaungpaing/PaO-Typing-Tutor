# Custom sounds

Copy your audio files into this folder and set their filenames in
`sounds.json`. The available events are `correct`, `incorrect`, and `complete`.
WAV, MP3, OGG, and other formats supported by your Qt installation can be used.

Missing or invalid files automatically use the built-in generated tone. Restart
the app after changing the manifest or files.
